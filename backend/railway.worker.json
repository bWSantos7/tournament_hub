{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "nixpacks.toml"
  },
  "deploy": {
    "startCommand": ". /opt/venv/bin/activate && python manage.py setup_periodic_tasks && celery -A config worker --beat --loglevel=info --concurrency=4 --max-tasks-per-child=100 --scheduler django_celery_beat.schedulers:DatabaseScheduler",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
